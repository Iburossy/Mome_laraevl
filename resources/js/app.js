import './bootstrap'; // Conserve les configurations par défaut de Laravel (Axios, etc.)
import 'bootstrap/dist/js/bootstrap.bundle.min.js'; // Import Bootstrap JS
import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();
